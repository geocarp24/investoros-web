# We Buy Ugly Houses Wisconsin — Scan 2026-05-07

## Changes Summary
Baseline scan, no diff. Capturing reference state for future comparisons:
- Page returns HTTP 200, title `"Sell My Wisconsin House Fast | We Buy Ugly Houses®"`, H1 `"Wisconsin Locations"`.
- Hero positions trust signal via tenure + volume: `"29 years of experience buying over 150,000 houses"` and routes to a locations page with zip/city search.
- 4 CTAs detected, two are cash-offer variants (`"Get a Cash Offer"`, `"GET A CASH OFFER"`), plus `"Request a Consultation"` and `"Share Your Feedback"`.
- Single national phone: `866-200-6475` (no local WI numbers on this page).
- Offer language limited to `"As Is | as is"`. No pricing disclosed.
- Social presence: Facebook, Instagram, YouTube, LinkedIn (HomeVestors parent on LinkedIn).
- No JSON-LD / schema markup captured in first 3000 chars.
- Word count 707 — locations-hub page, not a heavy SEO landing.

## change_severity: 0/10

## Recommended Action
- Programador — store this scan as `baseline_2026-05-07.json` under `agents/espia/snapshots/wbuh-wi/` within 24h so future diffs have a reference. Owner: Programador. Timeline: today.
- Scout — pull the full WBUH WI locations list (zip/city directory) on next scan to catalog their geographic coverage vs Pinnacle's R9 footprint. Owner: Scout. Timeline: next scheduled scan.
- SM Manager — note the `"29 years / 150,000 houses"` trust framing; Pinnacle counter-positioning should lead with *local Wisconsin* + *faster close* vs national-franchise tenure. Owner: SM Manager. Timeline: next content cycle.
- ALEX — flag absence of JSON-LD as a potential SEO weakness to exploit; Pinnacle landing pages should ship `RealEstateAgent` + `LocalBusiness` schema. Owner: ALEX. Timeline: next landing page deploy.

## New Pages Detected
- (baseline — no diff available; sitemap not crawled this scan)

## Removed Pages
- (baseline — no diff available)

## Schema Changes
- No structured data observed in the first 3000 chars of HTML. Recommend deeper schema fetch on next scan to confirm whether JSON-LD exists later in the document.

## Ad Copy Samples (if visible)
- Hero: `"We have 29 years of experience buying over 150,000 houses. To find a local property specialist, go to our locations page and search by zip code or city name."`
- Primary CTA: `"Get a Cash Offer"` / `"GET A CASH OFFER"` (case variants — likely sticky header + body button)
- Secondary CTA: `"Request a Consultation"`
- Offer keyword: `"As Is"` (only pricing/offer signal on page)
